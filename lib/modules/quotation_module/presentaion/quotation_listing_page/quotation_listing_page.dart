import '../../application/quotation_listing/use_case_manager.dart';

class QuotationListingPageWidget {
  QuotationListingManager manager = QuotationListingManager();

  //katai
  build() {
    manager.quotationList.first.images;
  }
}
