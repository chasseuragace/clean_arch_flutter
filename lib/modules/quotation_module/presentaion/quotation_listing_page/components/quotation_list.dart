/*
  Widget build(BuildContext context) {
      //provider of chatmanager  bata userManagerPauxa
      // userFromViewModel.User viewModelUser = UserListingManager.user;
       return ListView.builder(
          itemCount: viewModelUser.length,
          itemBuilder:(c,i){
          return Card(
          color: viewModelUser.isSingle?Colors.green:Colors.red,
          child:ListTile(
          title:Text(viewModelUser.name),
      


*/
